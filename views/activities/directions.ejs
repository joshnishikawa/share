<%- include('../head'); -%>

<!-- 
  This activity uses audio from the user's device, translates it to text, and compares it to the correct answer.
 -->

<style>
  #grid img, #display img{
    position:absolute;
    margin: auto;
    left: 0;
    right: 0;
    text-align: center;
    max-width:90%;
    max-height:90%;
  }
</style>

<script>
$(function(){
  let answer = "This is a test.";
  let answerWords = answer.split(" ");
  let answerWordsLength = answerWords.length;
  let answerWordsIndex = 0;
  let answerWordsIndexMax = answerWordsLength - 1;
  let answerWordsIndexMin = 0;

  let recognition = new webkitSpeechRecognition();
  recognition.continuous = true;
  recognition.interimResults = true;
  recognition.lang = 'en-US';

  
  
});
</script>
<%- include('../foot') %>